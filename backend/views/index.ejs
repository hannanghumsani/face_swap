<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>AI Face Swap</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="container">
      <h1>AI Face Swap</h1>
      <form action="/submit" method="POST" enctype="multipart/form-data">
        <div class="form-group">
          <label for="name">Name:</label>
          <input
            type="text"
            id="name"
            name="name"
            value="<%= values ? values.name : '' %>"
            required
          />
          <% if (errors && errors.name) { %>
          <p class="error"><%= errors.name %></p>
          <% } %>
        </div>

        <div class="form-group">
          <label for="email">Email:</label>
          <input
            type="email"
            id="email"
            name="email"
            value="<%= values ? values.email : '' %>"
            required
          />
          <% if (errors && errors.email) { %>
          <p class="error"><%= errors.email %></p>
          <% } %>
        </div>

        <div class="form-group">
          <label for="phone">Phone:</label>
          <input
            type="tel"
            id="phone"
            name="phone"
            value="<%= values ? values.phone : '' %>"
            required
          />
          <% if (errors && errors.phone) { %>
          <p class="error"><%= errors.phone %></p>
          <% } %>
        </div>

        <div class="form-group">
          <label>Your Photo (Original):</label>
          <input
            type="file"
            id="original-file"
            name="originalFile"
            accept="image/jpeg,image/png"
            required
          />
          <div id="original-preview" class="image-preview">
            <p>Your photo preview...</p>
          </div>
          <% if (errors && errors.originalFile) { %>
          <p class="error"><%= errors.originalFile %></p>
          <% } %>
        </div>

        <div class="form-group">
          <label>Face to Swap:</label>
          <input
            type="file"
            id="swap-file"
            name="swapFile"
            accept="image/jpeg,image/png"
            required
          />
          <div id="swap-preview" class="image-preview">
            <p>Face to swap preview...</p>
          </div>
          <% if (errors && errors.swapFile) { %>
          <p class="error"><%= errors.swapFile %></p>
          <% } %>
        </div>

        <div class="form-group checkbox">
          <input type="checkbox" id="tc" name="tc" <%= values && values.tc ?
          'checked' : '' %> required>
          <label for="tc">I accept the Terms & Conditions</label>
          <% if (errors && errors.tc) { %>
          <p class="error"><%= errors.tc %></p>
          <% } %>
        </div>

        <div class="form-group">
          <% if (errors && errors.general) { %>
          <p class="error"><%= errors.general %></p>
          <% } %>
          <button type="submit">Submit</button>
        </div>
      </form>
    </div>

    <div id="loader-container" class="loader-container">
      <div class="loader"></div>
      <div class="loader-text">Processing your request...</div>
    </div>

    <script src="/js/main.js"></script>
  </body>
</html>
